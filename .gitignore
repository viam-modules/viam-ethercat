# Build / install output
build*/
build-tsan/
install/
*.tar.gz

# Conan build process (bin/setup.sh + bin/build.sh) working files
tmp_cpp_sdk/
protobuf-override.profile
CMakeUserPresets.json
conanbuild*.sh
conanrun*.sh
deactivate_conan*.sh

# Local architecture-docs site (never committed; served over LAN for the team)
docs-site/

# Editor / OS cruft
.vscode/*
*~
.DS_Store
logs
.dir-locals.el
review-notes.org

# Local agent/tool settings (never tracked; add -A kept re-sweeping these)
.claude/settings.local.json

# Read-only third-party reference sources (each carries its own nested .git
# history and licensing; ~120MB). Kept on disk for the team to read, but not
# tracked in this repo.
reference-src/
build-hw/
.venv*/
__pycache__/

# Bench/internal material kept on disk but deliberately UNTRACKED (2026-07-06):
# docs, plans, bench tools + their tests, example configs, vendor manuals.
# CMakeLists gates the dependent targets on file existence, so this machine
# builds everything while a fresh clone builds the library + module + pure tests.
/docs/
/plan.md
/tools/
/scripts/
/a6-manuals/
/src/tools/
/etc/
/tests/a6_control_test.cpp
/tests/sine_move_test.cpp
/tests/a6_config_parse_test.cpp
